#!/usr/bin/env node
import{readFile}from'node:fs/promises';import{runExperiment}from'../public/faultline-engine.mjs';
if(!process.argv[2]||process.argv[2]==='--help'){console.log('Faultline: node scripts/faultline.mjs experiment.json [--max-error-rate 1]\nPrints a full simulated report. No real infrastructure is contacted.');}else{try{const input=JSON.parse(await readFile(process.argv[2],'utf8')),r=runExperiment(input.config??input);process.stdout.write(JSON.stringify(r,null,2)+'\n');const i=process.argv.indexOf('--max-error-rate');if(i!==-1){const raw=process.argv[i+1],n=Number(raw);if(raw===undefined||!Number.isFinite(n)||n<0||n>100)throw new Error('Expected an error-rate percentage from 0 to 100.');if(r.experiment.summary.failed/r.experiment.summary.total*100>n)process.exitCode=1;}}catch(e){console.error(e instanceof Error?e.message:'Simulation failed.');process.exitCode=2;}}
